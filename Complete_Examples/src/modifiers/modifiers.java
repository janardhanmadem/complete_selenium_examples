package modifiers;

public class modifiers {
	
	//public is accessible every where
	//default is accessible in the current package or with in the package
	//private is accessible only in the current class
	//protected is accessible in all child class and classes in current pack.

}
